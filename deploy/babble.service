# systemd unit for Babble — keeps the server running, restarts on crash/reboot.
#
# Install:
#   1. Edit User=, WorkingDirectory=, and the node path below to match your box.
#      Find your node path with:  which node
#   2. sudo cp deploy/babble.service /etc/systemd/system/babble.service
#   3. sudo systemctl daemon-reload
#   4. sudo systemctl enable --now babble
#   5. journalctl -u babble -f      # live logs

[Unit]
Description=Babble game server
After=network.target

[Service]
Type=simple
User=mrinmoy
WorkingDirectory=/home/mrinmoy/babble
# Use the absolute path from `which node` (nvm installs live elsewhere):
ExecStart=/usr/bin/node server.js
Environment=NODE_ENV=production
Environment=PORT=3000
# Only listen on localhost since nginx fronts it (remove if you expose 3000 directly):
Environment=HOST=127.0.0.1
# TTS backend: auto (default) | piper | say | espeak.
# For natural neural voices, install piper + voice models (see README) and set:
#   Environment=PIPER_VOICES_DIR=/home/mrinmoy/piper-voices
#   Environment=PIPER_PY=/home/mrinmoy/piper/bin/python
#   Environment=PIPER_DEFAULT_VOICE=en_US-amy-medium
# Drop any number of *.onnx voices in that folder; players pick one in the lobby.
# The model stays resident (a small Python sidecar), so synthesis is ~20ms.
# Without piper it falls back to espeak-ng (sudo apt install espeak-ng).
Restart=on-failure
RestartSec=3
# light hardening
NoNewPrivileges=true
ProtectSystem=full

[Install]
WantedBy=multi-user.target

